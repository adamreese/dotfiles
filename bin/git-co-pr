#!/usr/bin/env bash
set -euo pipefail

remote="upstream"
if ! git remote | grep upstream >/dev/null; then
  remote="origin"
fi

id="${1}"
branch="pr/${id}"

git fetch -fu "${remote}" "pull/${id}/head:${branch}"
git checkout "${branch}"
