# vt
# -----------------------------------------------------------------------------
# shortcut open in remote neovim tab

typeset -a _args=(.)
if (( $# )); then
  _args=($@)
fi

vr --remote-tab $_args

# -----------------------------------------------------------------------------
# vim:ft=zsh
