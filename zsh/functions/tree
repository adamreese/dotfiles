# ------------------------------------------------------------------------------
# tree
#
# Set tree defaults when no options are passed
# ------------------------------------------------------------------------------

typeset -a _args=(-aC --dirsfirst -I '.git')
if (( $# )); then
  _args+=($@)
else
  _args+=(-L 2)
fi

command tree $_args

# ------------------------------------------------------------------------------
# vim:ft=zsh
