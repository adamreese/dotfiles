---
#
# Global config for golangci-linter
#

run:
  concurrency: 4
  deadline: 2m

output:
  print-issued-lines: false
  format: line-number

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: true

  golint:
    min-confidence: 0.3

  gofmt:
    simplify: true

  misspell:
    locale: US

  gocritic:
    enabled-tags:
      - diagnostic
      - style
      - performance
      - experimental
    settings:
      captLocal:
        paramsOnly: true

linters:
  enable-all: true
  disable:
    - dupl
    - funlen
    - gochecknoglobals
    - gochecknoinits
    - goconst
    - gocyclo
    - gosec
    - lll
    - typecheck
    - wsl
